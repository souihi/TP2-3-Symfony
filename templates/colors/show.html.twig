{% extends 'base.html.twig' %}

{% block title %}Color{% endblock %}

{% block body %}
    <h1>Color</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ color.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ color.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_colors_index') }}">back to list</a>

    <a href="{{ path('app_colors_edit', {'id': color.id}) }}">edit</a>

    {{ include('colors/_delete_form.html.twig') }}
{% endblock %}

{% extends 'base.html.twig' %}

{% block title %}Pen{% endblock %}

{% block body %}
    <h1>Pen</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ pen.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ pen.name }}</td>
            </tr>
            <tr>
                <th>Price</th>
                <td>{{ pen.price }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ pen.description }}</td>
            </tr>
            <tr>
                <th>Ref</th>
                <td>{{ pen.ref }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_pens_index') }}">back to list</a>

    <a href="{{ path('app_pens_edit', {'id': pen.id}) }}">edit</a>

    {{ include('pens/_delete_form.html.twig') }}
{% endblock %}
